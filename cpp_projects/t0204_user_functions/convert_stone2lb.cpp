#include <iostream>

using namespace std;

int stonetolb(int);

// This program converts a weight in stones to pounds.
// It prompts the user to input a weight in stones and then calculates  
int main()
{
    int stone;
    cout << "Enter weight in stones: ";
    cin >> stone;
    int pounds;
    // TODO: Call the stonetolb function with the user input and display the result.
    // Write code here to convert the input stones to pounds and print the output.

    return 0;
}

// This function takes an integer value representing the weight in stones and returns
// the equivalent weight in pounds as an integer.
// The conversion factor is 14 pounds per stone.
// TODO: Define the stonetolb function here.
// Write a function named stonetolb that takes an integer parameter (number of stones)
// and returns the equivalent weight in pounds as an integer.
// Remember: 1 stone = 14 pounds.